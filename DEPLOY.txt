RACE BIB LOGGER v19.3.6 — DEPLOYMENT

1. Deploy every file in this package together. Do not mix v19.3.6 index.html, sw.js, or app/ files with an older release.
2. Replace Code.gs in the bound Apps Script project and deploy a new web-app version if the backend is hosted separately.
3. Publish index.html, manifest.json, sw.js, tailwind.css, icons/, and the complete app/ directory.
4. Confirm GitHub Pages or the chosen web host serves files from the same relative paths used in index.html.
5. Open the website once in a normal browser tab and refresh.
6. Fully close installed iPhone/Android PWAs and reopen them. The v19.3.6 service worker rotates the old cache automatically.
7. Verify:
   - Quick Entry remark opens the phone keyboard directly from 123 mode.
   - Unknown BIB prompt is compact and has no reason selector.
   - Removed Director widgets do not appear in the screen or Customize list.
   - OpenStreetMap loads, pans, and zooms.
   - A sample BIB logs locally and synchronizes.

No Racelog sheet migration is required for v19.3.6.
