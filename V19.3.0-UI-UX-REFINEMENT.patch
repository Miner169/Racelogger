diff -ruN '--exclude=*.zip' '--exclude=*.sha256' '--exclude=SHA256SUMS.txt' '--exclude=PREVIEW-*' '--exclude=*.patch' race-bib-logger-v19.2.1/Code.gs race-bib-logger-v19.3.0/Code.gs
--- race-bib-logger-v19.2.1/Code.gs	2026-08-02 07:03:10.604949019 +0000
+++ race-bib-logger-v19.3.0/Code.gs	2026-08-02 07:07:23.316958158 +0000
@@ -14,7 +14,7 @@
 const CHECKPOINT_GPS_HEADERS = ["Checkpoint", "Race KM", "Category", "Latitude", "Longitude", "Checkpoint KM", "Radius M", "Active", "Aliases", "Notes"];
 const CHECKPOINT_GPS_DEFAULT_RADIUS_M = 250;
 const CHECKPOINT_GPS_SPAM_DISTANCE_M = 25000;
-const APP_VERSION = "19.2.1";
+const APP_VERSION = "19.3.0";
 const LOCK_TIMEOUT_MS = 30000;
 const HEADERS = ["BIB", "Time", "Checkpoint", "Volunteer", "Remark", "Device", "Latitude", "Longitude", "UID", "Server Received At", "Status", "Status Changed At", "Passage Mode", "Creator ID", "Duplicate Of UID", "Duplicate Device Count", "Checkpoint KM", "Checkpoint KM Source", "Original Device Time", "Clock Offset Ms", "Clock Confidence Ms", "App Version", "Reason Code", "Reconciliation Flags", "Route Exception Reason", "Unknown BIB", "Record Checksum", "Edited At", "Edited By"];
 const LAST_ROW_CACHE_KEY = 'lastDataRow';
diff -ruN '--exclude=*.zip' '--exclude=*.sha256' '--exclude=SHA256SUMS.txt' '--exclude=PREVIEW-*' '--exclude=*.patch' race-bib-logger-v19.2.1/DEPLOY.txt race-bib-logger-v19.3.0/DEPLOY.txt
--- race-bib-logger-v19.2.1/DEPLOY.txt	2026-08-02 07:03:10.544949017 +0000
+++ race-bib-logger-v19.3.0/DEPLOY.txt	2026-08-02 07:16:50.244978658 +0000
@@ -1,44 +1,24 @@
-RACE BIB LOGGER v19.2.1 — UI, SYNC, AND INTERACTIVE MAP UPDATE
-================================================================
+RACE BIB LOGGER v19.3.0 — UI AND UX REFINEMENT
+==============================================
 
 1. BACK UP
 ----------
 - Copy the production Google Sheet and Apps Script project.
-- Export each device's pending BIB queue.
-- Retain the current production Web App deployment until acceptance passes.
+- Export pending BIB queues from active devices.
+- Keep the current production Web App deployment available until acceptance passes.
 - Run: npm test
 
 2. GOOGLE APPS SCRIPT
 ---------------------
-1) Replace Code.gs with the supplied v19.2.1 Code.gs.
+1) Replace Code.gs with the supplied v19.3.0 Code.gs.
 2) Keep Fornula cell.gs only when the existing project uses it.
-3) Save and deploy a NEW Web App version. Keep the existing Web App URL where possible.
-4) Confirm the PWA connection test reports the new server version.
+3) Save and deploy a NEW Web App version.
+4) Keep the existing Web App URL where possible.
+5) Confirm the PWA connection test reports server version 19.3.0.
 
 Racelog remains A:AC. No spreadsheet migration is required.
 
-3. GOOGLE MAPS CONFIGURATION
-----------------------------
-The Director GPS map needs a Google Maps JavaScript API browser key.
-
-Recommended server-managed setup:
-1) In Google Cloud, enable Maps JavaScript API for the selected project.
-2) Create a browser API key.
-3) Restrict the key to the exact PWA website/referrer and Maps JavaScript API.
-4) In Apps Script Project Settings > Script Properties, add:
-   GOOGLE_MAPS_BROWSER_API_KEY = your restricted browser key
-5) Optional, for a configured cloud map style:
-   GOOGLE_MAPS_MAP_ID = your map ID
-6) Redeploy the Apps Script Web App.
-7) In PWA Settings, run both the cloud connection test and Google Maps test.
-
-Per-device alternative:
-- Enter the restricted browser key and optional Map ID in PWA Settings.
-- These values remain on that device and override the server-provided values.
-
-Never embed an unrestricted or server-secret key in the static application files.
-
-4. STATIC PWA HOST
+3. STATIC PWA HOST
 ------------------
 Upload the complete package, including:
 - index.html, sw.js, tailwind.css, manifest.json
@@ -46,39 +26,55 @@
 - icons/
 
 New cache names:
-- race-logger-static-v19-2-1-r1
-- race-logger-runtime-v19-2-1-r1
+- race-logger-static-v19-3-0-r1
+- race-logger-runtime-v19-3-0-r1
+
+The new app-shell asset is:
+- app/ux-v193.js
 
-Open each installed PWA online once and confirm the new service worker activates.
-A full close/reopen may be required on iPhone after the hard-refresh action.
+Open each installed PWA online once so the new service worker can activate. On iPhone, fully close and reopen the installed PWA after the update if the old layout remains cached.
 
-5. ACCEPTANCE CHECKS
---------------------
-- Director Mode does not show incident timeline, incident board, missing-runner,
-  medical-capacity, transport/sweep, weather-risk, or supply widgets.
-- Header does not display incident, safety-note, or acknowledgement totals.
-- Unknown BIBs log without a confirmation prompt and appear in Reconciliation.
-- Duplicate and route-exception confirmations still work.
-- Frozen Safety Log BIB column stays narrow for short BIBs and widens for long BIBs.
-- A legacy successful sync response without checksum fields does not create a false
-  Sync Issue; an explicit checksum mismatch is still rejected.
-- Google map renders street tiles and supports drag, pinch/wheel zoom, map type,
-  Street View, fullscreen, checkpoint markers, PWA markers, and recent trails.
-- Map shows a useful configuration/error message when the key is missing or blocked.
-- BIB logging remains usable when Google Maps is unavailable.
-
-6. PHYSICAL DEVICE TESTS
-------------------------
-Test at minimum:
-- Recent and older iPhone installed PWA
-- Recent and low-memory Android installed PWA
-- Tablet and landscape layouts
-- Large text and reduced motion
-- Offline cold-open and reconnection
-- Weak/variable network and repeated foreground/background transitions
-- Real GPS, denied GPS, stale GPS, and inaccurate GPS
-- Short numeric and long alphanumeric BIBs
-- Two or more devices synchronizing at the same checkpoint
-- Restricted Google Maps key on the actual production hostname
+4. GOOGLE MAPS
+--------------
+The Director GPS map still requires the configured Google Maps JavaScript API browser key. No key is embedded in this package. Follow docs/GOOGLE-MAPS-SETUP-v19.3.0.md.
+
+5. UI ACCEPTANCE CHECKS
+-----------------------
+Quick Entry:
+- At 390 × 844, all 12 numeric keypad buttons, Clear, LOG, and the status line fit without overlap.
+- Entering BIB / Entering Remark changes correctly.
+- Phone keyboard remains closed in minimalist mode.
+- Screen Wake Lock status is informative but never blocks logging.
+- Repeated BIB colour and duplicate review remain intact.
+
+Main Entry:
+- OCR/keyboard controls do not shift the BIB text unexpectedly.
+- Success confirmation is compact, shows BIB/checkpoint, and does not cover the entry controls.
+- LOG remains usable with large text and on older iPhones.
+
+Director Mode:
+- Four command controls fit across a phone without horizontal clipping.
+- Pressing a command icon shows a floating label for about five seconds without resizing the bar.
+- Exit still requires two presses.
+- Section navigation jumps to visible sections and follows scrolling.
+- Back to Top appears only after scrolling.
+- Widget help toggles work on compact screens.
+- GPS map full-screen mode opens and closes without losing map interaction.
+
+Safety Log:
+- Frozen BIB width auto-fits short and long BIB labels.
+- Horizontal scrolling keeps the BIB visible.
+- The swipe hint disappears after use.
+
+6. OPERATIONAL REGRESSION CHECKS
+--------------------------------
+- BIB logging, duplicate review, unknown-BIB silent logging, route exceptions, GPS validation, offline queueing, reconciliation, safety notes, and sync checksums behave as in v19.2.1.
+- Google Maps failure never blocks BIB logging.
+- Offline cold-open succeeds after one online load.
+- No false Sync Issue is shown for successful compatible server responses.
+
+7. PHYSICAL DEVICE MATRIX
+-------------------------
+Use docs/DEVICE-TEST-MATRIX-v19.3.0.md. At minimum test recent and older iPhones, recent and low-memory Android devices, tablet, landscape, large text, reduced motion, offline/reconnection, weak network, real/stale/denied GPS, and multiple devices at one checkpoint.
 
 Do not deploy to a live race based only on local automated checks.
diff -ruN '--exclude=*.zip' '--exclude=*.sha256' '--exclude=SHA256SUMS.txt' '--exclude=PREVIEW-*' '--exclude=*.patch' race-bib-logger-v19.2.1/RELEASE-NOTES-v19.2.1.md race-bib-logger-v19.3.0/RELEASE-NOTES-v19.2.1.md
--- race-bib-logger-v19.2.1/RELEASE-NOTES-v19.2.1.md	2026-08-02 07:03:10.644949021 +0000
+++ race-bib-logger-v19.3.0/RELEASE-NOTES-v19.2.1.md	1970-01-01 00:00:00.000000000 +0000
@@ -1,96 +0,0 @@
-# Race Bib Logger v19.2.1
-
-## Purpose
-
-This release simplifies Director Mode, reduces race-day header noise, removes the unknown-runner interruption, improves compatibility with older deployed sync endpoints, auto-sizes the frozen BIB column, and replaces the GPS grid with an interactive Google Maps street map.
-
-## Director Mode changes
-
-Removed from the Director dashboard:
-
-- Command-centre incident timeline
-- Incident board
-- Missing-runner workflow
-- Medical capacity board
-- Sweep and transport tracking
-- Weather-risk panel
-- Checkpoint supply board
-
-Retained:
-
-- Arrival surge forecast
-- Checkpoint load heatmap
-- COT risk funnel
-- Route anomaly diagram
-- Finish projection
-- DNS, DNF, withdrawal, and medical totals
-- Checkpoint health
-- Shift handover
-- Post-race report
-- GPS/PWA device map
-
-The Director operations extension no longer opens, polls, or synchronizes CommandOps records. This removes unnecessary background traffic for the deleted workflows.
-
-## Interactive GPS map
-
-The Director GPS widget now uses the Google Maps JavaScript API and supports:
-
-- Street and satellite map types
-- Drag/pan
-- Mouse-wheel and pinch zoom
-- Zoom, Street View, fullscreen, scale, and map-type controls
-- Checkpoint markers
-- PWA/device markers
-- Recent GPS trails
-- Device detail popups
-- Fit-to-bounds across current race coordinates
-
-A Google Maps browser API key must be configured either locally in Settings or through Apps Script properties. No key is embedded in the release.
-
-## Quick Entry and reconciliation
-
-- A BIB not present in Setup is accepted immediately.
-- The record is silently flagged `unknown-bib` with reason code `UNKNOWN_NOT_IN_SETUP` for later reconciliation.
-- Duplicate passage warnings and checkpoint-sequence exception reasons remain unchanged.
-
-## Header and queue UI
-
-- The large Local/Server reconciliation badge was removed from the header.
-- The synchronization error indicator is now icon-only.
-- The header queue badge shows only pending BIB logs.
-- Incident, safety-note, acknowledgement, and checkpoint-status counts remain available in the queue inspector but no longer occupy the top bar.
-
-## Sync reliability
-
-- Explicit checksum mismatches still fail safely.
-- A successful response from an older deployed Apps Script endpoint that lacks the newer checksum acknowledgement fields is now accepted instead of being shown as a permanent false Sync Issue.
-- Operational data continues to synchronize in the background, but removed CommandOps dashboard workflows no longer create additional polling traffic.
-
-## Runner Safety Log
-
-The frozen BIB column now measures the displayed BIB values and adjusts between 72 and 220 pixels. It stays narrow for short BIBs and grows only when longer alphanumeric BIBs or route/collision badges require more room.
-
-## Backend and schema
-
-- App version: `19.2.1`
-- `Racelog` remains A:AC.
-- No spreadsheet schema migration is required.
-- Existing auxiliary sheets and legacy handlers remain intact for backward compatibility and historical data preservation.
-- Director operations responses no longer include removed incident, CommandOps, weather, medical, transport, missing-runner, or supply payloads.
-
-## Cache update
-
-- `race-logger-static-v19-2-1-r1`
-- `race-logger-runtime-v19-2-1-r1`
-
-## Validation completed
-
-- JavaScript syntax checks
-- Apps Script syntax check using a temporary `.js` copy
-- Deployment asset validation
-- Integrity unit tests
-- SHA-256 checksum parity test
-- FNV-1a fallback parity test
-- Static integration checks
-
-Physical iPhone/Android PWA, live Apps Script deployment, real GPS, Google Maps key/referrer restrictions, poor-network recovery, and multi-device soak testing remain required before race-day use.
diff -ruN '--exclude=*.zip' '--exclude=*.sha256' '--exclude=SHA256SUMS.txt' '--exclude=PREVIEW-*' '--exclude=*.patch' race-bib-logger-v19.2.1/RELEASE-NOTES-v19.3.0.md race-bib-logger-v19.3.0/RELEASE-NOTES-v19.3.0.md
--- race-bib-logger-v19.2.1/RELEASE-NOTES-v19.3.0.md	1970-01-01 00:00:00.000000000 +0000
+++ race-bib-logger-v19.3.0/RELEASE-NOTES-v19.3.0.md	2026-08-02 07:16:50.240978658 +0000
@@ -0,0 +1,69 @@
+# Race Bib Logger v19.3.0
+
+## UI and UX refinement release
+
+v19.3.0 keeps the v19.2.1 data model, synchronization contracts, Google Maps integration, duplicate review, route validation, reconciliation, and runner-safety logic. This release concentrates on race-day clarity, touch accuracy, reduced layout shift, and small-screen reliability.
+
+## Quick BIB Entry
+
+- Added an explicit **Entering BIB / Entering Remark** indicator above the entry field.
+- Added Screen Wake Lock support where the browser permits it, with a non-blocking status label.
+- Retuned the 390 × 844 layout so the full keypad, Clear, LOG, and status line fit without overlap.
+- Enlarged numeric touch targets while keeping the BIB preview compact.
+- Added short haptic feedback to keypad, tab, backspace, Clear, and LOG interactions when vibration is enabled.
+- Improved pressed states, spacing, shadows, safe-area handling, and focus visibility.
+- The action row remains in normal flow on compact phones, avoiding keypad obstruction.
+
+## Main logging screen
+
+- Preserved the BIB field as the primary surface and gave LOG a stable, large target.
+- Prevented the BIB field from shifting when OCR and keyboard controls become visible.
+- Improved focus rings and touch feedback.
+- Replaced the large centered success overlay with a compact, non-blocking confirmation showing the logged BIB and checkpoint.
+- Reduced success-confirmation duration to keep high-volume entry moving.
+
+## Race Command View
+
+- Reworked the command bar into fixed-size, equal-width controls on phones.
+- Button labels now appear in a floating five-second hint instead of expanding the toolbar and pushing controls off-screen.
+- Retained the two-tap Exit safeguard.
+- Added a horizontally scrollable section navigator for Categories, Standings, Map, COT, forecasts, device health, and other visible widgets.
+- Added automatic active-section highlighting while scrolling.
+- Added a floating Back to Top control after substantial scrolling.
+- Added per-widget information toggles on compact screens to reduce vertical clutter.
+- Added a full-screen mode for the interactive Google GPS map.
+- Added keyboard shortcuts in Director Mode: **Shift+R** refresh, **Shift+C** customize, and **Escape** arm/confirm exit.
+- Improved card elevation, header hierarchy, responsive spacing, and safe-area behavior.
+
+## Runner Safety Log
+
+- Retained the auto-fitted frozen BIB column.
+- Added a one-time swipe hint explaining that the BIB remains pinned during horizontal scrolling.
+- Improved row scanning with subtle alternating backgrounds and clearer hover/focus states.
+
+## Accessibility and motion
+
+- Added consistent high-visibility `:focus-visible` rings.
+- Preserved 44 px minimum touch targets where component-specific sizing does not override them.
+- Added reduced-motion handling for the new animations and scrolling effects.
+- Converted key mobile dialogs to bottom sheets for better reachability and keyboard avoidance.
+
+## Technical changes
+
+- Added `app/ux-v193.js` as an isolated enhancement layer.
+- Added the new module to the service-worker app shell.
+- Rotated caches to `race-logger-static-v19-3-0-r1` and `race-logger-runtime-v19-3-0-r1`.
+- Updated deployment validation and static integration checks.
+- No Racelog or auxiliary-sheet migration is required.
+
+## Validation performed
+
+- Deployment asset validation
+- JavaScript syntax checks
+- Integrity, checksum-parity, and FNV fallback tests
+- Static integration tests
+- Isolated headless-browser UX smoke test for the Director toolbar, section navigation, help toggles, map full-screen mode, and error-free initialization
+- 390 × 844 Quick Entry viewport fit check
+- 430 × 932 Director mobile layout preview
+
+Physical installed-PWA testing on iPhone, Android, tablet, real GPS, live Apps Script, production Google Maps credentials, and weak-network conditions remains required before race-day deployment.
diff -ruN '--exclude=*.zip' '--exclude=*.sha256' '--exclude=SHA256SUMS.txt' '--exclude=PREVIEW-*' '--exclude=*.patch' race-bib-logger-v19.2.1/TEST-RESULTS-v19.2.1.txt race-bib-logger-v19.3.0/TEST-RESULTS-v19.2.1.txt
--- race-bib-logger-v19.2.1/TEST-RESULTS-v19.2.1.txt	2026-08-02 07:03:10.560949018 +0000
+++ race-bib-logger-v19.3.0/TEST-RESULTS-v19.2.1.txt	1970-01-01 00:00:00.000000000 +0000
@@ -1,40 +0,0 @@
-RACE BIB LOGGER v19.2.1 — VALIDATION RESULTS
-============================================
-
-Automated checks completed in the build environment:
-
-PASS  node --check app/main.js
-PASS  node --check app/operations-v19.js
-PASS  node --check app/director-ops-v192.js
-PASS  node --check app/constants.js
-PASS  node --check sw.js
-PASS  node --check temporary JavaScript copy of Code.gs
-PASS  npm run validate:assets
-PASS  Integrity unit tests
-PASS  SHA-256 checksum parity
-PASS  FNV-1a 64-bit fallback parity
-PASS  Static integration checks
-
-Static integration coverage includes:
-- App/backend version alignment
-- A:AC Racelog schema preservation
-- Silent unknown-BIB reconciliation flags
-- Duplicate and route-exception workflows
-- Legacy successful sync-response compatibility
-- Explicit checksum mismatch protection
-- Removed Director widget list
-- Interactive Google Maps loader and greedy gesture handling
-- Google Maps Apps Script property payload
-- Auto-sized sticky BIB column
-- Service-worker cache version and application assets
-
-Not completed in this environment:
-- Installed iPhone PWA testing
-- Installed Android PWA testing
-- Live Google Apps Script write/sync testing
-- Valid production Google Maps API key and hostname restriction testing
-- Real GPS movement and trail testing
-- Poor-network, background-sync, and multi-device soak testing
-- Accessibility testing with VoiceOver/TalkBack
-
-Result: automated release checks passed. Physical and live-backend acceptance is still required.
diff -ruN '--exclude=*.zip' '--exclude=*.sha256' '--exclude=SHA256SUMS.txt' '--exclude=PREVIEW-*' '--exclude=*.patch' race-bib-logger-v19.2.1/TEST-RESULTS-v19.3.0.txt race-bib-logger-v19.3.0/TEST-RESULTS-v19.3.0.txt
--- race-bib-logger-v19.2.1/TEST-RESULTS-v19.3.0.txt	1970-01-01 00:00:00.000000000 +0000
+++ race-bib-logger-v19.3.0/TEST-RESULTS-v19.3.0.txt	2026-08-02 07:18:37.728982545 +0000
@@ -0,0 +1,74 @@
+RACE BIB LOGGER v19.3.0 — VALIDATION RESULTS
+============================================
+Date: 2026-08-02
+Scope: UI/UX refinement release based on v19.2.1
+
+AUTOMATED PACKAGE TESTS
+-----------------------
+PASS  Deployment asset validation
+      - 15 core files checked
+      - Manifest icons checked
+      - HTML local references checked
+      - Service-worker app-shell assets checked
+
+PASS  Integrity unit tests
+PASS  SHA-256 checksum parity fixture
+      abfa9fc0856dd88a2f527e091e4f05b662133b03cd13faf3e3e79108469f0687
+PASS  FNV-1a 64-bit fallback parity
+PASS  Static integration checks
+PASS  JavaScript syntax checks for app/*.js, sw.js, tests/*.mjs and scripts/*.mjs
+
+HEADLESS BROWSER UX SMOKE TEST
+------------------------------
+PASS  UI enhancement layer initialized with zero page errors
+PASS  Director visible-section navigation built correctly
+PASS  Widget help controls were injected
+PASS  Toolbar action produced a floating status hint
+PASS  Toolbar button did not receive the old expanding-width class
+PASS  Google map widget entered full-screen presentation mode
+PASS  Safety Log horizontal-scroll hint was injected
+
+390 × 844 QUICK ENTRY FIT TEST
+------------------------------
+PASS  Shell client height: 844 px
+PASS  Shell scroll height: 844 px
+PASS  Keypad bottom: 683.14 px
+PASS  Action row top: 729.31 px
+PASS  Status bottom: 829.61 px
+PASS  Keypad/action overlap: false
+PASS  Full Quick Entry surface fits viewport: true
+
+STATIC REGRESSION CHECKS
+------------------------
+PASS  Apps Script version is 19.3.0
+PASS  Racelog remains A:AC / 29 columns
+PASS  Unknown BIB confirmation remains removed
+PASS  Duplicate and route-exception workflows remain present
+PASS  Google Maps interactive renderer remains present
+PASS  Sync checksum and operational-batch paths remain present
+PASS  New app/ux-v193.js is referenced by HTML and cached by the service worker
+PASS  Service-worker cache is versioned race-logger-static-v19-3-0-r1
+
+PREVIEW CHECKS
+--------------
+PASS  390 × 844 Quick Entry preview generated
+PASS  430 × 932 mobile Director preview generated
+
+NOT EXECUTED IN THIS ENVIRONMENT
+--------------------------------
+- Installed iPhone PWA testing
+- Installed Android PWA testing
+- Real Screen Wake Lock lifecycle on physical devices
+- Physical haptic feedback
+- Live Google Apps Script deployment
+- Production Google Maps API key/referrer restrictions
+- Real GPS movement, stale GPS and denied GPS
+- Weak-network/background/foreground recovery
+- Multiple-device race-day soak test
+- Accessibility review with VoiceOver and TalkBack
+
+CONCLUSION
+----------
+All available package, syntax, integrity, static integration, isolated browser UX,
+and target-viewport fit checks passed. Physical-device and live-service acceptance
+remain required before race-day deployment.
diff -ruN '--exclude=*.zip' '--exclude=*.sha256' '--exclude=SHA256SUMS.txt' '--exclude=PREVIEW-*' '--exclude=*.patch' race-bib-logger-v19.2.1/app/constants.js race-bib-logger-v19.3.0/app/constants.js
--- race-bib-logger-v19.2.1/app/constants.js	2026-08-02 06:51:06.752922845 +0000
+++ race-bib-logger-v19.3.0/app/constants.js	2026-08-02 07:07:23.316958158 +0000
@@ -2,7 +2,7 @@
 (function (global) {
   'use strict';
   global.RaceConfig = Object.freeze({
-    appVersion: '19.2.1',
+    appVersion: '19.3.0',
     dbName: 'RaceLoggerDB',
     queueBatchSize: 50,
     operationalBatchSize: 25,
diff -ruN '--exclude=*.zip' '--exclude=*.sha256' '--exclude=SHA256SUMS.txt' '--exclude=PREVIEW-*' '--exclude=*.patch' race-bib-logger-v19.2.1/app/director-ops-v192.js race-bib-logger-v19.3.0/app/director-ops-v192.js
--- race-bib-logger-v19.2.1/app/director-ops-v192.js	2026-08-02 07:03:10.620949020 +0000
+++ race-bib-logger-v19.3.0/app/director-ops-v192.js	2026-08-02 07:07:23.324958158 +0000
@@ -1,11 +1,11 @@
-/* Race Bib Logger v19.2.1 director analytics suite.
+/* Race Bib Logger v19.3.0 director analytics suite.
  * Keeps high-value forecasting, safety analysis and reporting while removing
  * the operational boards requested for a cleaner command view.
  */
 (function (global) {
   'use strict';
 
-  const VERSION = '19.2.1';
+  const VERSION = '19.3.0';
   const STORE_KEY = 'raceCommandOps_v19_2';
   const HEATMAP_WINDOW_KEY = 'directorHeatmapWindow_v19_2';
   const OPS_EPOCH_KEY = 'raceCommandOpsEventEpoch_v19_2';
diff -ruN '--exclude=*.zip' '--exclude=*.sha256' '--exclude=SHA256SUMS.txt' '--exclude=PREVIEW-*' '--exclude=*.patch' race-bib-logger-v19.2.1/app/main.js race-bib-logger-v19.3.0/app/main.js
--- race-bib-logger-v19.2.1/app/main.js	2026-08-02 06:48:28.556917124 +0000
+++ race-bib-logger-v19.3.0/app/main.js	2026-08-02 07:09:32.268962821 +0000
@@ -1,4 +1,4 @@
-        const APP_VERSION = "19.2.1";
+        const APP_VERSION = "19.3.0";
         const DEFAULT_SYNC_URL = "https://script.google.com/macros/s/AKfycbzQQE7TLzm1muiHhBDrtenUZye0I8Yb2U3tNwq_3PsmtmvoddbeL11Kzm4P2RXqbCF_Ig/exec";
         let db;
         let dbReady_ = false;
@@ -2946,14 +2946,18 @@
             }
         }
 
-        function triggerSuccessToast() {
+        function triggerSuccessToast(bib = '', checkpoint = '') {
             const toast = document.getElementById("successToast");
             if (!toast) return;
+            const bibLabel = document.getElementById('successToastBib');
+            const detail = document.getElementById('successToastDetail');
+            if (bibLabel) bibLabel.textContent = bib ? `BIB ${bib} logged` : 'BIB logged';
+            if (detail) detail.textContent = checkpoint ? `${checkpoint} • saved on this device` : 'Saved on this device';
             clearTimeout(successToastTimeout);
             toast.classList.remove("animate-pop-scale", "hidden");
-            void toast.offsetWidth; 
+            void toast.offsetWidth;
             toast.classList.add("animate-pop-scale");
-            successToastTimeout = setTimeout(() => { hideSuccessToast(); }, 2000); 
+            successToastTimeout = setTimeout(() => { hideSuccessToast(); }, 1400);
         }
 
         function hideSuccessToast() {
@@ -3359,7 +3363,7 @@
                 loadHistory();
                 if (vibrateEnabled && navigator.vibrate) navigator.vibrate(50);
                 playSuccessSound();
-                triggerSuccessToast();
+                triggerSuccessToast(entry.bib, entry.checkpoint);
                 if (syncIntervalMs > 0) attemptSync();
                 requestBackgroundSync_();
                 announceToScreenReader_(entry.status === 'Location Spam'
diff -ruN '--exclude=*.zip' '--exclude=*.sha256' '--exclude=SHA256SUMS.txt' '--exclude=PREVIEW-*' '--exclude=*.patch' race-bib-logger-v19.2.1/app/ux-v193.js race-bib-logger-v19.3.0/app/ux-v193.js
--- race-bib-logger-v19.2.1/app/ux-v193.js	1970-01-01 00:00:00.000000000 +0000
+++ race-bib-logger-v19.3.0/app/ux-v193.js	2026-08-02 07:14:09.044972829 +0000
@@ -0,0 +1,362 @@
+/* Race Bib Logger v19.3.0 — non-invasive UI/UX enhancement layer.
+ * Loaded after the operational modules so it can improve presentation without
+ * changing record formats, sync contracts, or race calculations.
+ */
+(() => {
+  'use strict';
+
+  const VERSION = '19.3.0';
+  const DIRECTOR_LABEL_MS = 5000;
+  let directorHintTimer = 0;
+  let directorExitArmedUntil = 0;
+  let directorSectionObserver = null;
+  let minimalWakeLock = null;
+
+  function byId(id) { return document.getElementById(id); }
+  function storageGet(key) { try { return localStorage.getItem(key); } catch (_) { return null; } }
+  function storageSet(key, value) { try { localStorage.setItem(key, value); } catch (_) { /* storage unavailable */ } }
+  function isTextEntry(element) {
+    return !!element && /^(INPUT|TEXTAREA|SELECT)$/.test(element.tagName) || !!element?.isContentEditable;
+  }
+
+  function vibrate(pattern) {
+    try {
+      if (navigator.vibrate && storageGet('vibrateEnabled') !== 'false') navigator.vibrate(pattern);
+    } catch (_) { /* unsupported or blocked */ }
+  }
+
+  function showDirectorToolbarHint(label, durationMs = DIRECTOR_LABEL_MS) {
+    const hint = byId('directorToolbarHint');
+    if (!hint) return;
+    const clean = String(label || '').trim();
+    if (!clean) return;
+    hint.textContent = clean;
+    hint.classList.add('is-visible');
+    clearTimeout(directorHintTimer);
+    directorHintTimer = window.setTimeout(() => hint.classList.remove('is-visible'), Math.max(1200, Number(durationMs) || DIRECTOR_LABEL_MS));
+  }
+
+  function toolbarLabel(elementId, temporaryLabel) {
+    const element = byId(elementId);
+    if (!element) return '';
+    return String(temporaryLabel || element.dataset.fullLabel || element.getAttribute('aria-label') || '').trim();
+  }
+
+  function installStableDirectorToolbar() {
+    // Preserve the earlier five-second label requirement without changing button width.
+    // The label appears as an overlay, so the command bar never pushes off-screen.
+    window.revealDirectorToolbarLabel_ = function revealDirectorToolbarLabelV193(elementId, temporaryLabel, durationMs = DIRECTOR_LABEL_MS) {
+      showDirectorToolbarHint(toolbarLabel(elementId, temporaryLabel), durationMs);
+    };
+
+    window.directorCustomizeAction_ = function directorCustomizeActionV193(event) {
+      event?.preventDefault?.();
+      showDirectorToolbarHint('Customize command view');
+      if (typeof window.openDirectorCustomize === 'function') window.openDirectorCustomize(event);
+    };
+
+    window.directorRefreshAction_ = function directorRefreshActionV193(event) {
+      event?.preventDefault?.();
+      showDirectorToolbarHint('Refreshing race data…');
+      vibrate(12);
+      if (typeof window.pullServerRecords === 'function') window.pullServerRecords();
+      if (typeof window.attemptSync === 'function') window.attemptSync();
+    };
+
+    window.directorExitAction_ = function directorExitActionV193(event) {
+      event?.preventDefault?.();
+      const button = byId('directorExitButton');
+      const now = Date.now();
+      if (now < directorExitArmedUntil) {
+        directorExitArmedUntil = 0;
+        button?.classList.remove('is-armed');
+        showDirectorToolbarHint('Leaving Race Command View', 1200);
+        if (typeof window.closeDirectorMode === 'function') window.closeDirectorMode();
+        return;
+      }
+      directorExitArmedUntil = now + DIRECTOR_LABEL_MS;
+      button?.classList.add('is-armed');
+      button?.setAttribute('aria-label', 'Exit armed. Tap again within five seconds to leave Race Command View.');
+      showDirectorToolbarHint('Tap Exit again to leave', DIRECTOR_LABEL_MS);
+      window.setTimeout(() => {
+        if (Date.now() >= directorExitArmedUntil) {
+          button?.classList.remove('is-armed');
+          button?.setAttribute('aria-label', 'Exit Director Mode. Tap once to arm, tap again to exit.');
+        }
+      }, DIRECTOR_LABEL_MS + 50);
+    };
+  }
+
+  const DIRECTOR_SECTIONS = [
+    ['directorCategoryOverview', 'Categories'],
+    ['directorLeaderboardSection', 'Standings'],
+    ['widget-glance', 'At a glance'],
+    ['widget-chart', 'Category chart'],
+    ['widget-ticker', 'Live feed'],
+    ['widget-throughput', 'Checkpoint load'],
+    ['widget-progress', 'Progress'],
+    ['widget-flagged', 'Flags'],
+    ['widget-devices', 'Devices'],
+    ['widget-map', 'Map'],
+    ['widget-operations', 'Operations'],
+    ['widget-cot', 'COT risk'],
+    ['widget-forecast', 'Arrival forecast'],
+    ['widget-health', 'Finish projection'],
+    ['widget-checkpoint-health', 'Checkpoint health'],
+    ['widget-integrity', 'Integrity']
+  ];
+
+  function isVisible(element) {
+    if (!element || element.hidden || element.classList.contains('hidden')) return false;
+    const style = getComputedStyle(element);
+    return style.display !== 'none' && style.visibility !== 'hidden';
+  }
+
+  function buildDirectorSectionNav() {
+    const nav = byId('directorSectionNav');
+    const root = byId('directorModeView');
+    if (!nav || !root) return;
+    const available = DIRECTOR_SECTIONS
+      .map(([id, label]) => ({ id, label, element: byId(id) }))
+      .filter(item => isVisible(item.element));
+
+    nav.innerHTML = available.map((item, index) =>
+      `<button type="button" data-director-target="${item.id}" class="${index === 0 ? 'is-active' : ''}" aria-label="Jump to ${item.label}">${item.label}</button>`
+    ).join('');
+
+    nav.querySelectorAll('[data-director-target]').forEach(button => {
+      button.addEventListener('click', () => {
+        const target = byId(button.dataset.directorTarget);
+        if (!target) return;
+        target.scrollIntoView({ behavior: matchMedia('(prefers-reduced-motion: reduce)').matches ? 'auto' : 'smooth', block: 'start' });
+        nav.querySelectorAll('button').forEach(item => item.classList.toggle('is-active', item === button));
+        button.scrollIntoView({ behavior: 'smooth', block: 'nearest', inline: 'center' });
+        showDirectorToolbarHint(button.textContent, 1800);
+      });
+    });
+
+    directorSectionObserver?.disconnect();
+    if (!('IntersectionObserver' in window)) return;
+    directorSectionObserver = new IntersectionObserver(entries => {
+      const candidates = entries.filter(entry => entry.isIntersecting).sort((a, b) => b.intersectionRatio - a.intersectionRatio);
+      if (!candidates.length) return;
+      const activeId = candidates[0].target.id;
+      nav.querySelectorAll('button').forEach(button => {
+        const active = button.dataset.directorTarget === activeId;
+        button.classList.toggle('is-active', active);
+        if (active) button.scrollIntoView({ behavior: 'smooth', block: 'nearest', inline: 'nearest' });
+      });
+    }, { root, rootMargin: '-18% 0px -64% 0px', threshold: [0.08, 0.25, 0.5] });
+    available.forEach(item => directorSectionObserver.observe(item.element));
+  }
+
+  function installDirectorNavigation() {
+    const root = byId('directorModeView');
+    const backToTop = byId('directorBackToTop');
+    if (!root || !backToTop) return;
+
+    backToTop.addEventListener('click', () => {
+      root.scrollTo({ top: 0, behavior: matchMedia('(prefers-reduced-motion: reduce)').matches ? 'auto' : 'smooth' });
+      showDirectorToolbarHint('Top of command view', 1600);
+    });
+    root.addEventListener('scroll', () => backToTop.classList.toggle('is-visible', root.scrollTop > 650), { passive: true });
+
+    const grid = byId('directorWidgetsGrid');
+    if (grid && 'MutationObserver' in window) {
+      const observer = new MutationObserver(() => window.requestAnimationFrame(buildDirectorSectionNav));
+      observer.observe(grid, { subtree: true, attributes: true, attributeFilter: ['class', 'style', 'hidden'] });
+    }
+    buildDirectorSectionNav();
+  }
+
+  function installWidgetHelpToggles() {
+    document.querySelectorAll('#directorWidgetsGrid [data-widget]').forEach(section => {
+      const header = section.firstElementChild;
+      const description = header?.querySelector('p');
+      if (!header || !description || header.querySelector('.widget-help-button')) return;
+      description.classList.add('widget-help-text');
+      const button = document.createElement('button');
+      button.type = 'button';
+      button.className = 'widget-help-button';
+      button.textContent = 'i';
+      button.setAttribute('aria-label', 'Show or hide widget explanation');
+      button.setAttribute('aria-expanded', 'false');
+      button.addEventListener('click', event => {
+        event.stopPropagation();
+        const open = section.classList.toggle('show-widget-help');
+        button.setAttribute('aria-expanded', open ? 'true' : 'false');
+      });
+      const controls = header.querySelector('.widget-width-controls');
+      header.insertBefore(button, controls || null);
+    });
+  }
+
+  function installMapFullscreen() {
+    const widget = byId('widget-map');
+    const header = widget?.firstElementChild;
+    if (!widget || !header || header.querySelector('.map-fullscreen-button')) return;
+    const button = document.createElement('button');
+    button.type = 'button';
+    button.className = 'widget-help-button map-fullscreen-button';
+    button.textContent = '⛶';
+    button.setAttribute('aria-label', 'Open GPS map full screen');
+    button.setAttribute('aria-pressed', 'false');
+    button.addEventListener('click', event => {
+      event.stopPropagation();
+      const full = widget.classList.toggle('map-is-fullscreen');
+      if (full) document.body.classList.add('overflow-hidden');
+      else if (byId('directorModeView')?.classList.contains('hidden')) document.body.classList.remove('overflow-hidden');
+      button.textContent = full ? '✕' : '⛶';
+      button.setAttribute('aria-pressed', full ? 'true' : 'false');
+      button.setAttribute('aria-label', full ? 'Close full-screen GPS map' : 'Open GPS map full screen');
+      showDirectorToolbarHint(full ? 'GPS map full screen' : 'GPS map restored', 1600);
+      window.setTimeout(() => window.dispatchEvent(new Event('resize')), 180);
+    });
+    const controls = header.querySelector('.widget-width-controls');
+    header.insertBefore(button, controls || null);
+  }
+
+  function updateMinimalActiveTarget() {
+    const pill = byId('minimalActiveTargetPill');
+    const wrap = byId('minimalBibDisplayWrap');
+    const wakeStatus = byId('minimalWakeStatus');
+    if (pill) pill.textContent = wrap?.classList.contains('is-active') ? 'BIB' : 'REMARK';
+    if (wakeStatus && !('wakeLock' in navigator)) wakeStatus.textContent = 'Keep screen awake manually';
+  }
+
+  async function requestMinimalWakeLock() {
+    if (!('wakeLock' in navigator) || document.visibilityState !== 'visible') {
+      updateMinimalActiveTarget();
+      return;
+    }
+    try {
+      if (minimalWakeLock) return;
+      minimalWakeLock = await navigator.wakeLock.request('screen');
+      const status = byId('minimalWakeStatus');
+      if (status) status.textContent = 'Screen kept awake';
+      minimalWakeLock.addEventListener('release', () => {
+        minimalWakeLock = null;
+        const current = byId('minimalWakeStatus');
+        if (current) current.textContent = 'Screen awake';
+      });
+    } catch (_) {
+      const status = byId('minimalWakeStatus');
+      if (status) status.textContent = 'Keep screen awake manually';
+    }
+  }
+
+  async function releaseMinimalWakeLock() {
+    try { await minimalWakeLock?.release?.(); } catch (_) { /* already released */ }
+    minimalWakeLock = null;
+  }
+
+  function minimalViewIsOpen() {
+    const view = byId('minimalBibModeView');
+    return !!view && !view.classList.contains('hidden');
+  }
+
+  function installMinimalModeEnhancements() {
+    const view = byId('minimalBibModeView');
+    const bibWrap = byId('minimalBibDisplayWrap');
+    const remarkShell = byId('minimalRemarkShell');
+    if (!view) return;
+
+    view.addEventListener('pointerdown', event => {
+      const target = event.target.closest('.minimal-key, .minimal-bib-actions button, .minimal-backspace-btn, .minimal-keyboard-tab');
+      if (!target) return;
+      vibrate(target.id === 'minimalBibLogButton' ? 24 : 8);
+    }, { passive: true });
+
+    const observer = new MutationObserver(() => {
+      updateMinimalActiveTarget();
+      if (minimalViewIsOpen()) requestMinimalWakeLock();
+      else releaseMinimalWakeLock();
+    });
+    observer.observe(view, { attributes: true, attributeFilter: ['class'] });
+    if (bibWrap) observer.observe(bibWrap, { attributes: true, attributeFilter: ['class'] });
+    if (remarkShell) observer.observe(remarkShell, { attributes: true, attributeFilter: ['class'] });
+
+    document.addEventListener('visibilitychange', () => {
+      if (document.visibilityState === 'visible' && minimalViewIsOpen()) requestMinimalWakeLock();
+      else if (document.visibilityState !== 'visible') releaseMinimalWakeLock();
+    });
+    updateMinimalActiveTarget();
+  }
+
+  function installSafetyScrollHint() {
+    const viewport = byId('safetyTableViewport');
+    if (!viewport || byId('safetyScrollHint')) return;
+    const hint = document.createElement('div');
+    hint.id = 'safetyScrollHint';
+    hint.className = 'safety-scroll-hint';
+    hint.textContent = 'BIB stays pinned • swipe sideways for more columns';
+    viewport.parentNode?.insertBefore(hint, viewport);
+    const hide = () => {
+      if (viewport.scrollLeft > 12) {
+        hint.classList.add('is-hidden');
+        storageSet('safetyScrollHintSeen_v193', '1');
+      }
+    };
+    if (storageGet('safetyScrollHintSeen_v193') === '1') hint.classList.add('is-hidden');
+    viewport.addEventListener('scroll', hide, { passive: true });
+  }
+
+  function installDirectorKeyboardShortcuts() {
+    document.addEventListener('keydown', event => {
+      const root = byId('directorModeView');
+      if (!root || root.classList.contains('hidden') || isTextEntry(event.target)) return;
+      if (event.key === 'Escape') {
+        event.preventDefault();
+        window.directorExitAction_?.(event);
+      } else if (event.shiftKey && event.key.toLowerCase() === 'r') {
+        event.preventDefault();
+        window.directorRefreshAction_?.(event);
+      } else if (event.shiftKey && event.key.toLowerCase() === 'c') {
+        event.preventDefault();
+        window.directorCustomizeAction_?.(event);
+      }
+    }, true);
+  }
+
+  function installAppModeObserver() {
+    const director = byId('directorModeView');
+    if (!director || !('MutationObserver' in window)) return;
+    new MutationObserver(() => {
+      if (!director.classList.contains('hidden')) {
+        window.requestAnimationFrame(() => {
+          buildDirectorSectionNav();
+          installWidgetHelpToggles();
+          installMapFullscreen();
+        });
+      } else {
+        directorExitArmedUntil = 0;
+        byId('directorExitButton')?.classList.remove('is-armed');
+        byId('directorBackToTop')?.classList.remove('is-visible');
+        byId('directorToolbarHint')?.classList.remove('is-visible');
+        const mapWidget = byId('widget-map');
+        mapWidget?.classList.remove('map-is-fullscreen');
+        const mapButton = mapWidget?.querySelector('.map-fullscreen-button');
+        if (mapButton) {
+          mapButton.textContent = '⛶';
+          mapButton.setAttribute('aria-pressed', 'false');
+          mapButton.setAttribute('aria-label', 'Open GPS map full screen');
+        }
+      }
+    }).observe(director, { attributes: true, attributeFilter: ['class'] });
+  }
+
+  function init() {
+    document.documentElement.dataset.uxVersion = VERSION;
+    installStableDirectorToolbar();
+    installDirectorNavigation();
+    installWidgetHelpToggles();
+    installMapFullscreen();
+    installMinimalModeEnhancements();
+    installSafetyScrollHint();
+    installDirectorKeyboardShortcuts();
+    installAppModeObserver();
+  }
+
+  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', init, { once: true });
+  else init();
+})();
diff -ruN '--exclude=*.zip' '--exclude=*.sha256' '--exclude=SHA256SUMS.txt' '--exclude=PREVIEW-*' '--exclude=*.patch' race-bib-logger-v19.2.1/docs/DEVICE-TEST-MATRIX-v19.2.1.md race-bib-logger-v19.3.0/docs/DEVICE-TEST-MATRIX-v19.2.1.md
--- race-bib-logger-v19.2.1/docs/DEVICE-TEST-MATRIX-v19.2.1.md	2026-08-02 07:03:10.448949014 +0000
+++ race-bib-logger-v19.3.0/docs/DEVICE-TEST-MATRIX-v19.2.1.md	1970-01-01 00:00:00.000000000 +0000
@@ -1,83 +0,0 @@
-# Physical Device Test Matrix — v19.2.1
-
-Record device model, OS/browser version, build, date, tester, pass/fail, screenshots, and defect ID for every row.
-
-## Platforms
-
-| Platform | Required coverage |
-|---|---|
-| Recent iPhone | Current iOS Safari and installed PWA |
-| Older supported iPhone | Safari and installed PWA under memory pressure |
-| Recent Android | Chrome and installed PWA |
-| Low-memory Android | Chrome/PWA with background eviction testing |
-| Tablet | Portrait and landscape Director Mode |
-| Desktop command station | Chrome/Edge/Safari as applicable, large display and keyboard |
-
-## Quick Entry and synchronization
-
-- Enter configured and unconfigured BIBs; unconfigured BIBs must log without a prompt and appear in Reconciliation.
-- Trigger a duplicate and verify previous checkpoint, device, volunteer, elapsed time, Log anyway, and Cancel.
-- Trigger a route exception and verify a reason remains mandatory.
-- Queue BIBs offline; header shows only pending BIB count, not incident/safety/acknowledgement totals.
-- Reconnect through high latency, packet loss, intermittent Wi-Fi, and captive-portal conditions.
-- Test against the new v19.2.1 Apps Script deployment.
-- In staging only, test against the previous successful response shape without checksum fields; confirm no false permanent Sync Issue.
-- Return an explicit bad checksum acknowledgement; confirm the batch remains queued and the mismatch is surfaced.
-- Confirm server reconciliation count after recovery.
-
-## Runner Safety Log
-
-- Sort BIB ascending and descending.
-- Sort Last Seen newest-first and oldest-first.
-- Scroll horizontally and confirm the BIB column remains frozen.
-- Test 1–3 digit BIBs, 4–6 digit BIBs, and long alphanumeric BIBs.
-- Confirm the frozen column stays compact and expands only as needed.
-- Test route/collision badges in the frozen column.
-- Test large text and browser zoom at 200%.
-
-## Director Mode
-
-- Confirm these widgets are absent: incident timeline, incident board, missing runner, medical capacity, sweep/transport, weather risk, checkpoint supplies.
-- Confirm Arrival Forecast, heatmap, COT funnel, route anomalies, finish projection, outcomes, checkpoint health, handover, post-race report, and GPS map remain available.
-- Hide/show, reorder, resize, and reload retained widgets; confirm preferences persist.
-- Verify Arrival Forecast 10/20/30 labels and controlled traffic estimates.
-- Verify heatmap 60/120/240 windows, horizontal scrolling, sticky checkpoint column, and empty state.
-- Acknowledge and resolve COT alerts; reload and verify resolver/time/note.
-- Verify route skip, reverse, impossible-speed, and approved-exception examples.
-- Mark DNS, DNF, withdrawal, and medical statuses and verify totals.
-- Verify KM/category finish grouping and behavior when pace data is absent.
-- Generate/copy/download shift handover.
-- Export post-race HTML and JSON and open both files on another device.
-
-## Google Maps and GPS
-
-- Configure the restricted production browser key through Apps Script properties.
-- Test a local per-device key override, then remove it and confirm server-key fallback.
-- GPS allowed, denied, approximate, temporarily unavailable, and stale.
-- Verify Google street tiles render; no grid-only fallback remains.
-- Drag/pan with one finger or mouse.
-- Pinch and mouse-wheel zoom.
-- Test map-type, Street View, fullscreen, scale, and zoom controls.
-- Verify checkpoint markers, PWA markers, detail popups, marker age states, and recent trails.
-- Verify battery/charging, connection, queue, GPS age/accuracy, and last sync under the map.
-- Test invalid key, blocked referrer, API-disabled, quota error, and offline behavior.
-- Confirm map failure never blocks BIB entry or synchronization.
-- Test 1, 10, 30, and 100 reporting devices.
-
-## Accessibility and usability
-
-- Large text / browser zoom at 200%.
-- Reduced motion.
-- VoiceOver and TalkBack labels for controls and status changes.
-- Keyboard navigation and visible focus.
-- Wet/gloved touch use where relevant.
-- High-glare and dark environments.
-- Colour-independent status comprehension.
-
-## Load and soak
-
-- Two-hour multi-device soak with continuous BIB scans, COT alerts, device health, and Director refresh.
-- At least 10 simultaneous logging devices and two Director devices in a test environment.
-- Monitor Apps Script execution failures, lock contention, Sheets write rate, memory growth, battery drain, queue growth, Google Maps quota, and report generation time.
-
-Do not approve race-day deployment until all mandatory rows pass or have a documented operational workaround accepted by the race director and safety lead.
diff -ruN '--exclude=*.zip' '--exclude=*.sha256' '--exclude=SHA256SUMS.txt' '--exclude=PREVIEW-*' '--exclude=*.patch' race-bib-logger-v19.2.1/docs/DEVICE-TEST-MATRIX-v19.3.0.md race-bib-logger-v19.3.0/docs/DEVICE-TEST-MATRIX-v19.3.0.md
--- race-bib-logger-v19.2.1/docs/DEVICE-TEST-MATRIX-v19.3.0.md	1970-01-01 00:00:00.000000000 +0000
+++ race-bib-logger-v19.3.0/docs/DEVICE-TEST-MATRIX-v19.3.0.md	2026-08-02 07:17:16.216979597 +0000
@@ -0,0 +1,104 @@
+# Physical Device Test Matrix — v19.3.0
+
+Record device model, OS/browser version, build, date, tester, pass/fail, screenshots, and defect ID for every row.
+
+## Platforms
+
+| Platform | Required coverage |
+|---|---|
+| Recent iPhone | Current iOS Safari and installed PWA |
+| Older supported iPhone | Safari and installed PWA under memory pressure |
+| Recent Android | Chrome and installed PWA |
+| Low-memory Android | Chrome/PWA with background eviction testing |
+| Tablet | Portrait and landscape Director Mode |
+| Desktop command station | Chrome/Edge/Safari as applicable, large display and keyboard |
+
+## Quick Entry and synchronization
+
+- Enter configured and unconfigured BIBs; unconfigured BIBs must log without a prompt and appear in Reconciliation.
+- Trigger a duplicate and verify previous checkpoint, device, volunteer, elapsed time, Log anyway, and Cancel.
+- Trigger a route exception and verify a reason remains mandatory.
+- Queue BIBs offline; header shows only pending BIB count, not incident/safety/acknowledgement totals.
+- Reconnect through high latency, packet loss, intermittent Wi-Fi, and captive-portal conditions.
+- Test against the new v19.3.0 Apps Script deployment.
+- In staging only, test against the previous successful response shape without checksum fields; confirm no false permanent Sync Issue.
+- Return an explicit bad checksum acknowledgement; confirm the batch remains queued and the mismatch is surfaced.
+- Confirm server reconciliation count after recovery.
+
+## Runner Safety Log
+
+- Sort BIB ascending and descending.
+- Sort Last Seen newest-first and oldest-first.
+- Scroll horizontally and confirm the BIB column remains frozen.
+- Test 1–3 digit BIBs, 4–6 digit BIBs, and long alphanumeric BIBs.
+- Confirm the frozen column stays compact and expands only as needed.
+- Test route/collision badges in the frozen column.
+- Test large text and browser zoom at 200%.
+
+## Director Mode
+
+- Confirm these widgets are absent: incident timeline, incident board, missing runner, medical capacity, sweep/transport, weather risk, checkpoint supplies.
+- Confirm Arrival Forecast, heatmap, COT funnel, route anomalies, finish projection, outcomes, checkpoint health, handover, post-race report, and GPS map remain available.
+- Hide/show, reorder, resize, and reload retained widgets; confirm preferences persist.
+- Verify Arrival Forecast 10/20/30 labels and controlled traffic estimates.
+- Verify heatmap 60/120/240 windows, horizontal scrolling, sticky checkpoint column, and empty state.
+- Acknowledge and resolve COT alerts; reload and verify resolver/time/note.
+- Verify route skip, reverse, impossible-speed, and approved-exception examples.
+- Mark DNS, DNF, withdrawal, and medical statuses and verify totals.
+- Verify KM/category finish grouping and behavior when pace data is absent.
+- Generate/copy/download shift handover.
+- Export post-race HTML and JSON and open both files on another device.
+
+## Google Maps and GPS
+
+- Configure the restricted production browser key through Apps Script properties.
+- Test a local per-device key override, then remove it and confirm server-key fallback.
+- GPS allowed, denied, approximate, temporarily unavailable, and stale.
+- Verify Google street tiles render; no grid-only fallback remains.
+- Drag/pan with one finger or mouse.
+- Pinch and mouse-wheel zoom.
+- Test map-type, Street View, fullscreen, scale, and zoom controls.
+- Verify checkpoint markers, PWA markers, detail popups, marker age states, and recent trails.
+- Verify battery/charging, connection, queue, GPS age/accuracy, and last sync under the map.
+- Test invalid key, blocked referrer, API-disabled, quota error, and offline behavior.
+- Confirm map failure never blocks BIB entry or synchronization.
+- Test 1, 10, 30, and 100 reporting devices.
+
+## Accessibility and usability
+
+- Large text / browser zoom at 200%.
+- Reduced motion.
+- VoiceOver and TalkBack labels for controls and status changes.
+- Keyboard navigation and visible focus.
+- Wet/gloved touch use where relevant.
+- High-glare and dark environments.
+- Colour-independent status comprehension.
+
+## Load and soak
+
+- Two-hour multi-device soak with continuous BIB scans, COT alerts, device health, and Director refresh.
+- At least 10 simultaneous logging devices and two Director devices in a test environment.
+- Monitor Apps Script execution failures, lock contention, Sheets write rate, memory growth, battery drain, queue growth, Google Maps quota, and report generation time.
+
+Do not approve race-day deployment until all mandatory rows pass or have a documented operational workaround accepted by the race director and safety lead.
+
+## v19.3.0 UI/UX acceptance additions
+
+| Area | Scenario | Expected result |
+|---|---|---|
+| Quick Entry | 390 × 844 portrait | Full numeric keypad, Clear, LOG, and status fit with no overlap. |
+| Quick Entry | Switch BIB ↔ Remark | Active target pill updates immediately; phone keyboard stays closed. |
+| Quick Entry | Screen remains open | Wake Lock status updates accurately; failure never blocks entry. |
+| Quick Entry | Rapid 30-BIB sequence | Haptics remain brief, taps are not dropped, and success confirmation never covers keys. |
+| Main Entry | Focus BIB field | OCR/keyboard tools appear without shifting the entered BIB unexpectedly. |
+| Success feedback | Log numeric and long alphanumeric BIBs | Compact toast shows the correct BIB and checkpoint and clears quickly. |
+| Director phone | 390–430 px width | Exit, sync, customize, and refresh all remain visible with no horizontal page scroll. |
+| Director command labels | Tap each toolbar icon | Floating label is readable for about five seconds; button widths remain unchanged. |
+| Director navigation | Scroll and tap section chips | Active chip follows viewport and tap scrolls to the chosen visible section. |
+| Director map | Enter/exit full screen | Map remains draggable/zoomable and returns to its widget without a blank canvas. |
+| Director keyboard | Shift+R, Shift+C, Escape | Refresh, Customize, and guarded Exit work when focus is not in a form control. |
+| Safety Log | Short and long BIBs | Frozen BIB column remains narrow for short labels and expands only as needed. |
+| Safety Log | Horizontal swipe | BIB stays pinned and one-time swipe hint disappears after use. |
+| Accessibility | Keyboard navigation | Every actionable control has a visible focus ring. |
+| Accessibility | Reduced Motion | New transitions and smooth scrolling are suppressed. |
+| Mobile dialogs | Duplicate/recovery/customize | Dialog appears as a reachable bottom sheet and respects the bottom safe area. |
diff -ruN '--exclude=*.zip' '--exclude=*.sha256' '--exclude=SHA256SUMS.txt' '--exclude=PREVIEW-*' '--exclude=*.patch' race-bib-logger-v19.2.1/docs/GOOGLE-MAPS-SETUP-v19.2.1.md race-bib-logger-v19.3.0/docs/GOOGLE-MAPS-SETUP-v19.2.1.md
--- race-bib-logger-v19.2.1/docs/GOOGLE-MAPS-SETUP-v19.2.1.md	2026-08-02 07:03:10.552949018 +0000
+++ race-bib-logger-v19.3.0/docs/GOOGLE-MAPS-SETUP-v19.2.1.md	1970-01-01 00:00:00.000000000 +0000
@@ -1,32 +0,0 @@
-# Google Maps setup — v19.2.1
-
-The Director GPS widget uses the Google Maps JavaScript API. The release does not contain an API key.
-
-## Recommended setup
-
-1. Select or create a Google Cloud project.
-2. Enable **Maps JavaScript API**.
-3. Create a browser API key.
-4. Add an application restriction for the exact PWA hostname/referrer.
-5. Add an API restriction allowing only **Maps JavaScript API**.
-6. In Apps Script, open **Project Settings → Script Properties** and add:
-   - `GOOGLE_MAPS_BROWSER_API_KEY`
-   - optional: `GOOGLE_MAPS_MAP_ID`
-7. Deploy a new Apps Script Web App version.
-8. Open the PWA, run the cloud connection test, and then press **Test Google Maps**.
-
-## Per-device override
-
-PWA Settings also accepts a browser key and optional Map ID. A local value overrides the server-provided value. This can be useful for staging, but server-managed configuration is easier to maintain across many race devices.
-
-## Security
-
-- Use a browser key, not a server-secret key.
-- Restrict the key to the exact production hostname/referrer.
-- Restrict it to Maps JavaScript API.
-- Use separate keys for production and testing.
-- Review billing and quota alerts before race day.
-
-## Failure behaviour
-
-A missing, invalid, blocked, or offline map does not block BIB logging. The Director widget shows an actionable error while the rest of the PWA remains operational.
diff -ruN '--exclude=*.zip' '--exclude=*.sha256' '--exclude=SHA256SUMS.txt' '--exclude=PREVIEW-*' '--exclude=*.patch' race-bib-logger-v19.2.1/docs/GOOGLE-MAPS-SETUP-v19.3.0.md race-bib-logger-v19.3.0/docs/GOOGLE-MAPS-SETUP-v19.3.0.md
--- race-bib-logger-v19.2.1/docs/GOOGLE-MAPS-SETUP-v19.3.0.md	1970-01-01 00:00:00.000000000 +0000
+++ race-bib-logger-v19.3.0/docs/GOOGLE-MAPS-SETUP-v19.3.0.md	2026-08-02 07:16:50.124978654 +0000
@@ -0,0 +1,32 @@
+# Google Maps setup — v19.3.0
+
+The Director GPS widget uses the Google Maps JavaScript API. The release does not contain an API key.
+
+## Recommended setup
+
+1. Select or create a Google Cloud project.
+2. Enable **Maps JavaScript API**.
+3. Create a browser API key.
+4. Add an application restriction for the exact PWA hostname/referrer.
+5. Add an API restriction allowing only **Maps JavaScript API**.
+6. In Apps Script, open **Project Settings → Script Properties** and add:
+   - `GOOGLE_MAPS_BROWSER_API_KEY`
+   - optional: `GOOGLE_MAPS_MAP_ID`
+7. Deploy a new Apps Script Web App version.
+8. Open the PWA, run the cloud connection test, and then press **Test Google Maps**.
+
+## Per-device override
+
+PWA Settings also accepts a browser key and optional Map ID. A local value overrides the server-provided value. This can be useful for staging, but server-managed configuration is easier to maintain across many race devices.
+
+## Security
+
+- Use a browser key, not a server-secret key.
+- Restrict the key to the exact production hostname/referrer.
+- Restrict it to Maps JavaScript API.
+- Use separate keys for production and testing.
+- Review billing and quota alerts before race day.
+
+## Failure behaviour
+
+A missing, invalid, blocked, or offline map does not block BIB logging. The Director widget shows an actionable error while the rest of the PWA remains operational.
diff -ruN '--exclude=*.zip' '--exclude=*.sha256' '--exclude=SHA256SUMS.txt' '--exclude=PREVIEW-*' '--exclude=*.patch' race-bib-logger-v19.2.1/docs/IMPLEMENTATION-STATUS-v19.2.1.md race-bib-logger-v19.3.0/docs/IMPLEMENTATION-STATUS-v19.2.1.md
--- race-bib-logger-v19.2.1/docs/IMPLEMENTATION-STATUS-v19.2.1.md	2026-08-02 06:55:10.272931650 +0000
+++ race-bib-logger-v19.3.0/docs/IMPLEMENTATION-STATUS-v19.2.1.md	1970-01-01 00:00:00.000000000 +0000
@@ -1,36 +0,0 @@
-# v19.2.1 implementation status
-
-## Completed
-
-- Removed seven requested Director widgets from rendering and customization.
-- Disabled CommandOps polling/sync initialization for those removed workflows.
-- Removed the header reconciliation count badge.
-- Limited the top queue badge to pending BIB logs.
-- Made the sync-error badge icon-only.
-- Removed the unknown-runner interruption; unknown BIBs are silently flagged.
-- Added legacy successful-sync compatibility while retaining checksum mismatch rejection.
-- Added measured, bounded width for the frozen Safety Log BIB column.
-- Replaced the grid-only GPS renderer with an interactive Google Maps renderer.
-- Added local and Apps Script property configuration for Google Maps.
-- Reduced Director operations payload by removing unused incident, CommandOps, weather, medical, transport, missing-runner, and supply data.
-- Updated shift handover and post-race report to exclude removed workflows.
-- Updated service-worker caches, package metadata, deployment checks, and static tests.
-
-## Preserved
-
-- Duplicate confirmation and reason selection
-- Route-sequence exception reason requirement
-- Unknown-BIB reconciliation flagging
-- Offline BIB queue and operational data stores
-- COT alerts and safety action card
-- Arrival forecast, heatmap, COT funnel, route anomaly, finish projection, outcome totals, checkpoint health, handover, and report
-- Existing A:AC Racelog schema
-- Existing auxiliary sheet data and legacy backend handlers
-
-## Pending physical acceptance
-
-- iPhone and Android installed-PWA interaction
-- Production Google Maps key/referrer restrictions
-- Real GPS markers and trails
-- Live Apps Script legacy/current response testing
-- Poor-network and multi-device soak testing
diff -ruN '--exclude=*.zip' '--exclude=*.sha256' '--exclude=SHA256SUMS.txt' '--exclude=PREVIEW-*' '--exclude=*.patch' race-bib-logger-v19.2.1/docs/IMPLEMENTATION-STATUS-v19.3.0.md race-bib-logger-v19.3.0/docs/IMPLEMENTATION-STATUS-v19.3.0.md
--- race-bib-logger-v19.2.1/docs/IMPLEMENTATION-STATUS-v19.3.0.md	1970-01-01 00:00:00.000000000 +0000
+++ race-bib-logger-v19.3.0/docs/IMPLEMENTATION-STATUS-v19.3.0.md	2026-08-02 07:17:16.216979597 +0000
@@ -0,0 +1,49 @@
+# v19.3.0 implementation status
+
+## Completed in this release
+
+- Added a dedicated `app/ux-v193.js` enhancement layer without changing record formats or server actions.
+- Rebuilt the mobile Director toolbar as four stable, equal-width icon controls.
+- Moved five-second command labels into a floating overlay to eliminate toolbar layout shift.
+- Preserved two-tap Exit confirmation and added Director keyboard shortcuts.
+- Added visible-section navigation, active-section tracking, and Back to Top.
+- Added compact widget help toggles and full-screen Google GPS map presentation.
+- Added explicit BIB/Remark target indication to minimalist entry.
+- Added Screen Wake Lock support and non-blocking status reporting.
+- Added optional haptic feedback to minimalist controls.
+- Rebalanced the 390 × 844 Quick Entry layout so keypad and actions do not overlap.
+- Added compact BIB/checkpoint success confirmation instead of the large centered overlay.
+- Added Safety Log horizontal-swipe guidance while retaining the measured frozen BIB width.
+- Added focus-visible styling, reduced-motion support, safe-area handling, and mobile bottom-sheet dialogs.
+- Rotated service-worker caches and extended deployment/static validation for the new UI module.
+
+## Preserved from v19.2.1
+
+- Silent unknown-BIB logging and reconciliation flags
+- Detailed duplicate review
+- Route-sequence exception reasons
+- Offline queue, checksums, retry/backoff, and reconciliation
+- COT alerts, arrival forecast, heatmap, route anomalies, finish projection, outcome totals, checkpoint health, handover, and reporting
+- Interactive Google Maps renderer, checkpoint/PWA markers, trails, zoom, pan, Street View, and map-type controls
+- Auto-fitted frozen Safety Log BIB column
+- A:AC Racelog schema and existing auxiliary sheets
+
+## Not changed
+
+- Google Sheet schema
+- Apps Script endpoint contracts
+- Checksum algorithms
+- Runner-category calculations
+- GPS validation rules
+- Duplicate and location-spam lifecycle rules
+
+## Pending physical acceptance
+
+- Installed PWA on recent and older iPhones
+- Installed PWA on recent and low-memory Android devices
+- Screen Wake Lock behavior across foreground/background transitions
+- Haptic feedback settings and device support
+- Real GPS markers and trails with production Google Maps credentials
+- Live Apps Script current/legacy response testing
+- Large text, reduced motion, landscape, and tablet layouts
+- Poor-network, offline recovery, and multi-device soak testing
diff -ruN '--exclude=*.zip' '--exclude=*.sha256' '--exclude=SHA256SUMS.txt' '--exclude=PREVIEW-*' '--exclude=*.patch' race-bib-logger-v19.2.1/index.html race-bib-logger-v19.3.0/index.html
--- race-bib-logger-v19.2.1/index.html	2026-08-02 07:03:10.796949026 +0000
+++ race-bib-logger-v19.3.0/index.html	2026-08-02 07:15:14.308975189 +0000
@@ -3286,6 +3286,338 @@
         .v19-checkpoint-health-row { display:flex; align-items:center; justify-content:space-between; gap:.65rem; border-bottom:1px solid rgba(120,120,120,.16); padding:.55rem 0; }.v19-checkpoint-health-row>div{display:flex;flex-direction:column}.v19-checkpoint-health-row strong{font-size:.75rem}.v19-checkpoint-health-row span{font-size:.62rem;opacity:.68}.v19-checkpoint-status{border:1px solid rgba(120,120,120,.25);border-radius:.55rem;padding:.35rem .5rem;font-size:.65rem;font-weight:900;background:transparent}.v19-cp-operational{color:#059669}.v19-cp-busy{color:#d97706}.v19-cp-degraded,.v19-cp-offline{color:#dc2626}.v19-cp-closing{color:#7c3aed}.v19-cp-closed{color:#52525b}
         @media(max-width:700px){.v19-detail-grid,.v19-recon-grid,.v19-safety-card-grid{grid-template-columns:1fr}.v19-recon-wide{grid-column:auto}.v19-timeline-row{grid-template-columns:.65rem 1fr}.v19-timeline-row time{grid-column:2}.v19-recovery-row{grid-template-columns:2rem 1fr}.v19-recovery-row b{grid-column:2}.v19-checkpoint-health-row{align-items:flex-start;flex-direction:column}.v19-checkpoint-status{width:100%}}
 
+
+
+        /* =====================================================================
+           v19.3.0 — Race-day UI/UX refinement layer
+           Keeps operational behaviour unchanged while making primary actions
+           easier to hit, reducing layout shift, and improving small-screen use.
+           ===================================================================== */
+        :root {
+            --ux-focus-ring: rgba(96, 165, 250, .62);
+            --ux-raised-shadow: 0 12px 30px rgba(0, 0, 0, .22);
+            --ux-soft-shadow: 0 6px 18px rgba(0, 0, 0, .16);
+            --ux-header-glass: color-mix(in srgb, var(--header-bg) 90%, transparent);
+        }
+
+        :where(button, a, input, select, textarea, [tabindex]):focus-visible {
+            outline: 3px solid var(--ux-focus-ring) !important;
+            outline-offset: 2px;
+        }
+        :where(button, [role="button"]) { min-height: 44px; }
+        :where(button, [role="button"]):active { transform: translateY(1px) scale(.985); }
+        input, select, textarea { scroll-margin-top: 7rem; }
+
+        /* Compact, non-blocking confirmation. Logging feedback no longer covers the
+           bib keypad or the runner queue during rapid entry. */
+        #successToast {
+            top: auto !important;
+            left: 50% !important;
+            bottom: calc(env(safe-area-inset-bottom, 0px) + 1rem) !important;
+            transform: translateX(-50%) !important;
+            width: min(calc(100vw - 2rem), 25rem);
+            padding: .72rem .9rem !important;
+            border-radius: 1rem !important;
+            flex-direction: row !important;
+            align-items: center !important;
+            justify-content: flex-start !important;
+            gap: .7rem;
+            pointer-events: none;
+            background: color-mix(in srgb, var(--panel-bg) 90%, transparent) !important;
+            color: var(--text-main);
+            box-shadow: var(--ux-raised-shadow);
+        }
+        @keyframes successToastInV193 {
+            from { transform: translate(-50%, .7rem) scale(.96); opacity: 0; }
+            to { transform: translate(-50%, 0) scale(1); opacity: 1; }
+        }
+        #successToast.animate-pop-scale { animation: successToastInV193 .18s ease-out forwards; }
+        #successToast .success-toast-check {
+            width: 2.35rem;
+            height: 2.35rem;
+            display: grid;
+            place-items: center;
+            flex: 0 0 auto;
+            border-radius: 999px;
+            background: rgba(16, 185, 129, .16);
+            color: #34d399;
+            font-size: 1.35rem;
+            font-weight: 950;
+        }
+        #successToast .success-toast-copy { min-width: 0; display: flex; flex-direction: column; text-align: left; }
+        #successToastBib { font-size: .9rem; font-weight: 950; line-height: 1.1; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
+        #successToastDetail { margin-top: .15rem; font-size: .65rem; font-weight: 750; color: var(--text-muted); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
+
+        /* Main entry: preserve BIB as the dominant action while keeping LOG a large,
+           stable target. The field/tool layout never jumps when OCR tools appear. */
+        .bib-entry-submit-grid { grid-template-columns: minmax(0, 1fr) clamp(4.75rem, 18vw, 7rem); }
+        #logActionButton { min-height: 4rem; border-radius: .9rem; box-shadow: 0 8px 20px rgba(37, 99, 235, .22); }
+        .bib-input-shell:focus-within #bibInput,
+        .bib-input-shell.tools-visible #bibInput { padding-left: 5.65rem; padding-right: .8rem; }
+        #bibInput { border-radius: .9rem; font-variant-numeric: tabular-nums; }
+        .remark-input-row { align-items: stretch; }
+        .minimal-mode-toggle { min-width: 3rem; min-height: 3rem; border-radius: .8rem; }
+
+        /* Quick entry: explicit active target, thumb-friendly keypad, stable action row,
+           safe-area spacing and wake-lock status. */
+        #minimalBibModeView { background: #050506; }
+        .minimal-bib-shell {
+            width: min(100%, 42rem);
+            margin-inline: auto;
+            min-height: 100dvh;
+            padding-left: max(.9rem, env(safe-area-inset-left, 0px));
+            padding-right: max(.9rem, env(safe-area-inset-right, 0px));
+        }
+        .minimal-entry-mode-row {
+            display: flex;
+            align-items: center;
+            justify-content: space-between;
+            gap: .65rem;
+            min-height: 2.2rem;
+            padding: .15rem .15rem 0;
+            color: #9ca3af;
+            font-size: .63rem;
+            font-weight: 850;
+            letter-spacing: .08em;
+            text-transform: uppercase;
+        }
+        .minimal-entry-mode-copy { display: flex; align-items: center; gap: .45rem; min-width: 0; }
+        #minimalActiveTargetPill {
+            display: inline-flex;
+            align-items: center;
+            justify-content: center;
+            min-width: 4.35rem;
+            min-height: 1.8rem;
+            padding: .2rem .58rem;
+            border: 1px solid rgba(59, 130, 246, .62);
+            border-radius: 999px;
+            background: rgba(37, 99, 235, .18);
+            color: #93c5fd;
+            font-size: .65rem;
+            font-weight: 950;
+            letter-spacing: .08em;
+        }
+        #minimalWakeStatus { margin-left: auto; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
+        .minimal-bib-display-wrap {
+            box-shadow: inset 0 0 0 1px rgba(255,255,255,.025), 0 10px 28px rgba(0,0,0,.28);
+        }
+        .minimal-bib-display-wrap.is-active { box-shadow: 0 0 0 2px #3b82f6, 0 12px 28px rgba(37,99,235,.2); }
+        #minimalBibDisplay { font-variant-numeric: tabular-nums; text-wrap: balance; }
+        .minimal-keyboard-tabs { gap: .45rem; }
+        .minimal-keyboard-tab { min-height: 3rem; border-radius: .78rem; }
+        .minimal-key {
+            border-radius: .9rem !important;
+            box-shadow: inset 0 1px 0 rgba(255,255,255,.055), 0 5px 12px rgba(0,0,0,.23);
+            transition: transform .06s ease, filter .08s ease, border-color .12s ease;
+        }
+        .minimal-key:active { transform: translateY(2px) scale(.975) !important; filter: brightness(1.14); }
+        #minimalBibKeyboard.numbers-layout .minimal-key { min-height: clamp(4.15rem, 8.7dvh, 5.3rem); font-size: clamp(1.45rem, 5.2vw, 2rem); }
+        #minimalBibKeyboard.numbers-layout .minimal-key.utility,
+        #minimalBibKeyboard.numbers-layout .minimal-key.danger { font-size: clamp(.72rem, 2.8vw, .95rem); letter-spacing: .04em; }
+        .minimal-bib-actions {
+            position: sticky;
+            bottom: 0;
+            z-index: 5;
+            padding-top: .65rem;
+            padding-bottom: max(.65rem, env(safe-area-inset-bottom, 0px));
+            background: linear-gradient(to bottom, transparent, #050506 18%, #050506 100%);
+        }
+        .minimal-bib-actions button { min-height: clamp(4.2rem, 8dvh, 5.2rem); border-radius: 1rem; }
+        #minimalBibStatus { min-height: 1.35rem; padding-bottom: max(.15rem, env(safe-area-inset-bottom, 0px)); }
+
+        /* Director command bar: fixed-size icons eliminate the horizontal jump seen
+           when labels expand. Labels are now announced in a floating hint instead. */
+        .director-mode-header {
+            background: var(--ux-header-glass) !important;
+            -webkit-backdrop-filter: blur(18px) saturate(1.25);
+            backdrop-filter: blur(18px) saturate(1.25);
+            box-shadow: 0 8px 26px rgba(0,0,0,.2) !important;
+        }
+        .director-mode-actions { flex-wrap: nowrap !important; }
+        .director-icon-action,
+        .director-icon-action.is-expanded {
+            width: 2.75rem !important;
+            min-width: 2.75rem !important;
+            max-width: 2.75rem !important;
+            height: 2.75rem !important;
+            min-height: 2.75rem !important;
+            padding: 0 !important;
+            gap: 0 !important;
+            border-radius: .82rem !important;
+            flex: 0 0 2.75rem;
+        }
+        .director-icon-action .director-action-label,
+        .director-icon-action.is-expanded .director-action-label { display: none !important; }
+        .director-icon-action .director-action-icon { font-size: 1.15rem; }
+        .director-toolbar-hint {
+            position: fixed;
+            z-index: 115;
+            left: 50%;
+            top: max(calc(env(safe-area-inset-top, 0px) + 4.2rem), 4.2rem);
+            transform: translate(-50%, -.5rem);
+            max-width: min(90vw, 30rem);
+            padding: .55rem .82rem;
+            border: 1px solid var(--border-color);
+            border-radius: 999px;
+            background: color-mix(in srgb, var(--panel-bg) 92%, transparent);
+            color: var(--text-main);
+            box-shadow: var(--ux-raised-shadow);
+            font-size: .7rem;
+            font-weight: 900;
+            text-align: center;
+            opacity: 0;
+            pointer-events: none;
+            transition: opacity .15s ease, transform .18s ease;
+        }
+        .director-toolbar-hint.is-visible { opacity: 1; transform: translate(-50%, 0); }
+        .director-section-nav {
+            display: flex;
+            gap: .42rem;
+            overflow-x: auto;
+            padding: .62rem .15rem .1rem;
+            scrollbar-width: none;
+            scroll-snap-type: x proximity;
+        }
+        .director-section-nav::-webkit-scrollbar { display: none; }
+        .director-section-nav button {
+            min-height: 2.45rem;
+            flex: 0 0 auto;
+            scroll-snap-align: start;
+            padding: .42rem .72rem;
+            border: 1px solid var(--border-color);
+            border-radius: 999px;
+            background: var(--card-tint);
+            color: var(--text-muted);
+            font-size: .66rem;
+            font-weight: 850;
+            white-space: nowrap;
+        }
+        .director-section-nav button.is-active { color: #bfdbfe; border-color: rgba(59,130,246,.62); background: rgba(37,99,235,.2); }
+        #directorModeView section.theme-panel { box-shadow: var(--ux-soft-shadow); }
+        #directorModeView section.theme-panel > div:first-child { background: linear-gradient(90deg, color-mix(in srgb, var(--card-tint) 84%, #3b82f6 16%), var(--card-tint)) !important; }
+        .widget-help-button {
+            width: 2rem;
+            min-width: 2rem;
+            min-height: 2rem;
+            height: 2rem;
+            border-radius: 999px;
+            border: 1px solid var(--border-color);
+            color: var(--text-muted);
+            background: var(--input-bg);
+            font-size: .72rem;
+            font-weight: 950;
+        }
+        .widget-help-text { transition: max-height .18s ease, opacity .15s ease, margin .18s ease; }
+        .director-back-to-top {
+            position: fixed;
+            right: max(1rem, env(safe-area-inset-right, 0px));
+            bottom: max(1rem, env(safe-area-inset-bottom, 0px));
+            z-index: 80;
+            width: 3rem;
+            min-width: 3rem;
+            height: 3rem;
+            border-radius: 999px;
+            border: 1px solid rgba(96,165,250,.48);
+            background: rgba(29,78,216,.92);
+            color: white;
+            box-shadow: var(--ux-raised-shadow);
+            opacity: 0;
+            pointer-events: none;
+            transform: translateY(.6rem);
+            transition: opacity .15s ease, transform .15s ease;
+        }
+        .director-back-to-top.is-visible { opacity: 1; pointer-events: auto; transform: translateY(0); }
+        .map-fullscreen-button { margin-left: .2rem; }
+        #widget-map.map-is-fullscreen {
+            position: fixed !important;
+            inset: max(.5rem, env(safe-area-inset-top,0px)) max(.5rem, env(safe-area-inset-right,0px)) max(.5rem, env(safe-area-inset-bottom,0px)) max(.5rem, env(safe-area-inset-left,0px));
+            z-index: 110;
+            width: auto !important;
+            height: auto !important;
+            max-height: none !important;
+            grid-column: auto !important;
+            background: var(--panel-bg);
+            border-radius: 1rem;
+            box-shadow: 0 30px 90px rgba(0,0,0,.62);
+        }
+        #widget-map.map-is-fullscreen #directorMapBody { flex: 1; min-height: 0; overflow: auto; }
+        #widget-map.map-is-fullscreen .director-gps-map-stage { height: calc(100dvh - 10rem); min-height: 22rem; }
+
+        /* Safety table: pinned BIB remains compact, rows scan more easily, and a small
+           one-time hint explains horizontal scrolling without covering data. */
+        #safetyLogTable tbody tr:nth-child(even) td { background-color: color-mix(in srgb, var(--panel-bg) 96%, var(--header-text) 4%); }
+        #safetyLogTable tbody tr:hover td { background-color: color-mix(in srgb, var(--panel-bg) 86%, #3b82f6 14%); }
+        #safetyLogTable tbody td { min-height: 3rem; vertical-align: middle; }
+        .safety-scroll-hint {
+            position: sticky;
+            left: .55rem;
+            bottom: .55rem;
+            z-index: 8;
+            width: max-content;
+            max-width: calc(100vw - 2rem);
+            margin: .5rem;
+            padding: .42rem .62rem;
+            border: 1px solid var(--border-color);
+            border-radius: 999px;
+            background: color-mix(in srgb, var(--panel-bg) 92%, transparent);
+            color: var(--text-muted);
+            box-shadow: var(--ux-soft-shadow);
+            font-size: .62rem;
+            font-weight: 850;
+            pointer-events: none;
+            transition: opacity .18s ease;
+        }
+        .safety-scroll-hint.is-hidden { opacity: 0; }
+
+        /* Mobile modals become reachable bottom sheets instead of vertically centered
+           cards that fight the on-screen keyboard and safe areas. */
+        @media (max-width: 640px) {
+            #v19DecisionModal, #recoveryWizard, #directorCustomizeModal { align-items: flex-end !important; padding: 0 !important; }
+            #v19DecisionModal > div, #recoveryWizard > div, #directorCustomizeModal > div {
+                width: 100% !important;
+                max-width: none !important;
+                max-height: min(92dvh, 52rem) !important;
+                border-radius: 1.35rem 1.35rem 0 0 !important;
+                padding-bottom: max(1rem, env(safe-area-inset-bottom,0px)) !important;
+            }
+            .director-mode-header { grid-template-columns: minmax(0,1fr) !important; gap: .55rem !important; padding: max(.55rem, env(safe-area-inset-top,0px)) .7rem .65rem !important; }
+            .director-mode-title { font-size: .95rem !important; white-space: nowrap !important; }
+            .director-mode-actions { display: grid !important; grid-template-columns: repeat(4, minmax(0,1fr)); width: 100%; gap: .48rem !important; }
+            .director-icon-action, .director-icon-action.is-expanded { width: 100% !important; max-width: none !important; min-width: 0 !important; }
+            #directorExitButton { order: 0 !important; margin-right: 0 !important; }
+            .director-toolbar-hint { top: max(calc(env(safe-area-inset-top,0px) + 7.4rem), 7.4rem); }
+            #directorModeView > .p-3, #directorModeView > .p-5 { padding-left: .7rem !important; padding-right: .7rem !important; }
+            #directorWidgetsGrid { column-gap: .7rem !important; row-gap: .7rem !important; }
+            #directorWidgetsGrid [data-widget] > div:first-child p.widget-help-text { max-height: 0; opacity: 0; overflow: hidden; margin-top: 0 !important; }
+            #directorWidgetsGrid [data-widget].show-widget-help > div:first-child p.widget-help-text { max-height: 5rem; opacity: 1; margin-top: .25rem !important; }
+            .minimal-last4-panel { padding: .65rem; }
+            .minimal-last4-card { min-width: 0; }
+        }
+
+        @media (prefers-reduced-motion: reduce) {
+            *, *::before, *::after { animation-duration: .001ms !important; animation-iteration-count: 1 !important; scroll-behavior: auto !important; transition-duration: .001ms !important; }
+        }
+
+
+        @media (max-width: 430px) and (max-height: 900px) {
+            .minimal-bib-shell { gap: .42rem; }
+            .minimal-last4-panel { padding: .48rem; }
+            .minimal-last4-card { min-height: 3.3rem; padding: .4rem .25rem; }
+            .minimal-last4-card strong { font-size: clamp(1rem, 5.6vw, 1.35rem); }
+            .minimal-entry-mode-row { min-height: 1.75rem; padding-top: 0; }
+            #minimalActiveTargetPill { min-height: 1.55rem; min-width: 3.8rem; }
+            .minimal-entry-zone .minimal-bib-display-wrap { min-height: 5.9rem; max-height: 6.25rem; }
+            #minimalBibDisplay.minimal-bib-short { font-size: clamp(3.2rem, 17vw, 5.5rem); }
+            .minimal-remark-shell { padding: .4rem .52rem .46rem; }
+            #minimalRemarkInput { min-height: 2.15rem; padding: .42rem .62rem; }
+            .minimal-keyboard-tab { min-height: 2.7rem; }
+            #minimalBibKeyboard.numbers-layout { gap: .38rem; }
+            #minimalBibKeyboard.numbers-layout .minimal-key { min-height: 3.78rem; }
+            .minimal-bib-actions { position: relative; padding-top: .38rem; padding-bottom: max(.2rem, env(safe-area-inset-bottom,0px)); background: #050506; }
+            .minimal-bib-actions button { min-height: 3.85rem; }
+            #minimalBibStatus { font-size: .58rem; min-height: 1rem; }
+        }
+
     </style>
 </head>
 <body class="theme-bg theme-text min-h-screen lg:h-screen flex flex-col font-sans select-none pb-4 lg:pb-0 lg:overflow-hidden">
@@ -3662,6 +3994,10 @@
             </section>
 
             <div class="minimal-entry-zone">
+                <div class="minimal-entry-mode-row" aria-live="polite">
+                    <div class="minimal-entry-mode-copy"><span>Entering</span><strong id="minimalActiveTargetPill">BIB</strong></div>
+                    <span id="minimalWakeStatus">Screen awake</span>
+                </div>
                 <div id="minimalBibDisplayWrap" class="minimal-bib-display-wrap is-active" onclick="activateMinimalEntryTarget_('bib')">
                     <div id="minimalBibDisplay" class="is-placeholder" role="textbox" aria-label="BIB being entered" aria-live="polite">Tap the keypad</div>
                     <span id="minimalBibRepeatHint" class="minimal-bib-repeat-hint" aria-live="polite">Previously recorded</span>
@@ -3715,6 +4051,7 @@
                 </button>
             </div>
         </div>
+        <div id="directorToolbarHint" class="director-toolbar-hint" role="status" aria-live="polite"></div>
 
         <!-- Widget picker: lets the person watching this screen choose which panels are
              worth their space, with a one-line explanation of what each one shows. -->
@@ -3741,6 +4078,7 @@
         </div>
 
         <div class="p-3 sm:p-5 max-w-[1900px] mx-auto flex flex-col gap-3 sm:gap-4">
+            <nav id="directorSectionNav" class="director-section-nav" aria-label="Jump to command-view section"></nav>
 
             <!-- At-a-glance stat tiles -->
             <div id="directorStatTiles" class="grid grid-cols-3 lg:grid-cols-6 gap-2 sm:gap-3"></div>
@@ -3748,7 +4086,7 @@
             <div class="grid grid-cols-1 xl:grid-cols-[1.1fr_1.7fr] gap-3 sm:gap-4 items-start">
 
                 <!-- Category overview -->
-                <section class="theme-panel rounded-2xl border shadow overflow-hidden">
+                <section id="directorCategoryOverview" class="theme-panel rounded-2xl border shadow overflow-hidden">
                     <div class="px-4 py-3 border-b theme-border bg-neutral-200/50 dark:bg-neutral-900/10">
                         <h2 class="text-xs sm:text-sm font-black theme-text-muted uppercase tracking-wider">📊 Category Overview</h2>
                     </div>
@@ -3772,7 +4110,7 @@
                 </section>
 
                 <!-- Live leaderboard -->
-                <section class="theme-panel rounded-2xl border shadow overflow-hidden">
+                <section id="directorLeaderboardSection" class="theme-panel rounded-2xl border shadow overflow-hidden">
                     <div class="px-4 py-3 border-b theme-border bg-neutral-200/50 dark:bg-neutral-900/10 flex items-center justify-between gap-2">
                         <h2 class="text-xs sm:text-sm font-black theme-text-muted uppercase tracking-wider">🏆 Live Standings by Category</h2>
                         <span class="text-[9px] sm:text-[10px] theme-text-muted font-semibold">Ranked by furthest checkpoint, then total time</span>
@@ -3990,6 +4328,8 @@
          director/safety officer can account for every runner, not just
          review scan history.
          ══════════════════════════════════════════════════════════════════ -->
+    <button id="directorBackToTop" type="button" class="director-back-to-top" aria-label="Back to top of Race Command View" title="Back to top">↑</button>
+
     <div id="safetyLogView" class="hidden fixed inset-0 z-[60] theme-bg overflow-y-auto" style="padding-top: env(safe-area-inset-top, 0px);">
         <div class="max-w-[1400px] mx-auto p-4 flex flex-col gap-4">
             <div class="flex items-center justify-between gap-2 flex-wrap">
@@ -4548,10 +4888,13 @@
         </div>
     </main>
 
-    <!-- Transparent Success Toast -->
-    <div id="successToast" class="hidden fixed top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 z-[100] cursor-pointer bg-white/80 dark:bg-neutral-950/40 backdrop-blur-md rounded-2xl p-6 flex flex-col items-center justify-center select-none border border-neutral-300 dark:border-neutral-800 shadow-2xl" onclick="hideSuccessToast()">
-        <span class="text-7xl filter drop-shadow-md opacity-90 transform transition-transform active:scale-90">✅</span>
-        <span class="text-[10px] text-emerald-700 dark:text-emerald-400 font-black tracking-widest uppercase mt-1 opacity-95">Logged</span>
+    <!-- Compact, non-blocking success confirmation -->
+    <div id="successToast" class="hidden fixed z-[100] cursor-pointer backdrop-blur-md border shadow-2xl" onclick="hideSuccessToast()" role="alert" aria-live="assertive">
+        <span class="success-toast-check" aria-hidden="true">✓</span>
+        <span class="success-toast-copy">
+            <strong id="successToastBib">BIB logged</strong>
+            <span id="successToastDetail">Saved on this device</span>
+        </span>
     </div>
 
 
@@ -4624,5 +4967,6 @@
     <script src="app/main.js"></script>
     <script src="app/operations-v19.js"></script>
     <script src="app/director-ops-v192.js"></script>
+    <script src="app/ux-v193.js"></script>
 </body>
 </html>
diff -ruN '--exclude=*.zip' '--exclude=*.sha256' '--exclude=SHA256SUMS.txt' '--exclude=PREVIEW-*' '--exclude=*.patch' race-bib-logger-v19.2.1/package.json race-bib-logger-v19.3.0/package.json
--- race-bib-logger-v19.2.1/package.json	2026-08-02 06:51:06.752922845 +0000
+++ race-bib-logger-v19.3.0/package.json	2026-08-02 07:07:23.324958158 +0000
@@ -1,7 +1,7 @@
 {
-  "name": "race-bib-logger-v19-2-1",
+  "name": "race-bib-logger-v19-3-0",
   "private": true,
-  "version": "19.2.1",
+  "version": "19.3.0",
   "scripts": {
     "validate:assets": "node scripts/validate-assets.mjs",
     "test:unit": "node tests/unit-integrity.mjs && node tests/checksum-parity.mjs && node tests/fnv-parity.mjs",
diff -ruN '--exclude=*.zip' '--exclude=*.sha256' '--exclude=SHA256SUMS.txt' '--exclude=PREVIEW-*' '--exclude=*.patch' race-bib-logger-v19.2.1/scripts/validate-assets.mjs race-bib-logger-v19.3.0/scripts/validate-assets.mjs
--- race-bib-logger-v19.2.1/scripts/validate-assets.mjs	2026-08-02 06:52:11.584925189 +0000
+++ race-bib-logger-v19.3.0/scripts/validate-assets.mjs	2026-08-02 07:10:45.112965455 +0000
@@ -9,7 +9,7 @@
   'index.html', 'tailwind.css', 'manifest.json', 'sw.js', 'Code.gs',
   'app/constants.js', 'app/contracts.js', 'app/state-store.js', 'app/errors.js',
   'app/components.js', 'app/integrity.js', 'app/main.js', 'app/operations-v19.js',
-  'app/director-ops-v192.js'
+  'app/director-ops-v192.js', 'app/ux-v193.js'
 ];
 
 function exists(relative) {
@@ -52,7 +52,7 @@
   else {
     for (const match of cacheArray[1].matchAll(/["'](\.\/[^"']+)["']/g)) exists(match[1]);
   }
-  if (!/race-logger-static-v19-2-1/.test(sw)) errors.push('Service-worker static cache is not versioned for v19.2.1.');
+  if (!/race-logger-static-v19-3-0/.test(sw)) errors.push('Service-worker static cache is not versioned for v19.3.0.');
 } catch (error) {
   errors.push(`Could not inspect sw.js: ${error.message}`);
 }
diff -ruN '--exclude=*.zip' '--exclude=*.sha256' '--exclude=SHA256SUMS.txt' '--exclude=PREVIEW-*' '--exclude=*.patch' race-bib-logger-v19.2.1/sw.js race-bib-logger-v19.3.0/sw.js
--- race-bib-logger-v19.2.1/sw.js	2026-08-02 06:51:06.752922845 +0000
+++ race-bib-logger-v19.3.0/sw.js	2026-08-02 07:10:45.112965455 +0000
@@ -1,9 +1,9 @@
-// Race Bib Logger v19.2.1 UI, sync, and interactive-map revision.
+// Race Bib Logger v19.3.0 UI, sync, and interactive-map revision.
 // Keeps the existing IndexedDB/background-sync logic below unchanged while making
 // app-shell caching safe for subdirectory deployments and shared web origins.
 const CACHE_PREFIX = 'race-logger-';
-const STATIC_CACHE = 'race-logger-static-v19-2-1-r1';
-const RUNTIME_CACHE = 'race-logger-runtime-v19-2-1-r1';
+const STATIC_CACHE = 'race-logger-static-v19-3-0-r1';
+const RUNTIME_CACHE = 'race-logger-runtime-v19-3-0-r1';
 const NETWORK_TIMEOUT_MS = 4500;
 const MAX_RUNTIME_ENTRIES = 80;
 
@@ -25,6 +25,7 @@
   './app/main.js',
   './app/operations-v19.js',
   './app/director-ops-v192.js',
+  './app/ux-v193.js',
   './icons/icon-192.png',
   './icons/icon-512.png',
   './icons/icon-512-maskable.png'
diff -ruN '--exclude=*.zip' '--exclude=*.sha256' '--exclude=SHA256SUMS.txt' '--exclude=PREVIEW-*' '--exclude=*.patch' race-bib-logger-v19.2.1/tests/static-checks.mjs race-bib-logger-v19.3.0/tests/static-checks.mjs
--- race-bib-logger-v19.2.1/tests/static-checks.mjs	2026-08-02 06:52:11.584925189 +0000
+++ race-bib-logger-v19.3.0/tests/static-checks.mjs	2026-08-02 07:11:03.132966106 +0000
@@ -9,10 +9,11 @@
 const main = read('app/main.js');
 const ops = read('app/operations-v19.js');
 const command = read('app/director-ops-v192.js');
+const ux = read('app/ux-v193.js');
 const backend = read('Code.gs');
 const sw = read('sw.js');
 
-assert.match(backend, /const APP_VERSION = "19\.2\.1"/);
+assert.match(backend, /const APP_VERSION = "19\.3\.0"/);
 assert.match(backend, /const DATA_COLUMN_COUNT = 29/);
 assert.match(backend, /"Reason Code".*"Reconciliation Flags".*"Record Checksum"/s);
 assert.match(backend, /const COMMAND_OPS_SHEET_NAME = "CommandOps"/);
@@ -36,7 +37,7 @@
 for (const id of ['v19DecisionModal', 'clockDriftBlocker', 'reconciliationView', 'recoveryWizard', 'offlineSafetyActionCard', 'directorCheckpointHealthBody', 'directorMapBody', 'safetyBibHeader', 'safetyLastSeenHeader', 'minimalBibRepeatHint']) {
   assert.match(html, new RegExp(`id=["']${id}["']`), `Missing UI element #${id}`);
 }
-for (const file of ['constants', 'contracts', 'state-store', 'errors', 'components', 'integrity', 'main', 'operations-v19', 'director-ops-v192']) {
+for (const file of ['constants', 'contracts', 'state-store', 'errors', 'components', 'integrity', 'main', 'operations-v19', 'director-ops-v192', 'ux-v193']) {
   assert.match(html, new RegExp(`app/${file}\\.js`), `Missing module script ${file}.js`);
 }
 assert.ok(html.split('\n').length < 5000, 'index.html should remain presentation-first after module extraction.');
@@ -81,7 +82,15 @@
 assert.match(command, /gpsCapturedAt/);
 assert.match(sw, /app\/operations-v19\.js/);
 assert.match(sw, /app\/director-ops-v192\.js/);
-assert.match(sw, /race-logger-static-v19-2-1/);
+assert.match(sw, /app\/ux-v193\.js/);
+assert.match(sw, /race-logger-static-v19-3-0/);
+assert.match(ux, /showDirectorToolbarHint/);
+assert.match(ux, /requestMinimalWakeLock/);
+assert.match(ux, /buildDirectorSectionNav/);
+assert.match(ux, /installMapFullscreen/);
+assert.match(html, /id="directorSectionNav"/);
+assert.match(html, /id="minimalActiveTargetPill"/);
+assert.match(html, /id="successToastBib"/);
 assert.match(sw, /attachRecordChecksums_\(unsynced\)/);
 assert.match(sw, /checksumBatch_\(unsynced\)/);
 assert.match(sw, /operational_batch/);
